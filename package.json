{
  "name": "ninekeys-landing-page",
  "version": "1.0.0",
  "description": "Landing page institucional da NineKeys",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "(tsc || true) && mkdir -p assets && cp -R src/styles/*.css assets/",
    "sync:css": "mkdir -p assets && cp -R src/styles/*.css assets/",
    "type-check": "tsc --noEmit || echo 'No TypeScript files found - this is OK for now'",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
